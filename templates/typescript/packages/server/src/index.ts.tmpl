import { formatGreeting } from "@{{package_scope}}/common"
import { createConnectionString } from "@{{package_scope}}/db"

export function buildServerBanner(name: string): string {
  return `${formatGreeting(name)} Connected to ${createConnectionString("service")}`
}
