import { formatPackageLabel } from "@{{package_scope}}/common"

const MANAGED_PACKAGES = ["common", "server", "client", "db", "integration", "scripts"] as const

export function listManagedPackages(): string[] {
  return MANAGED_PACKAGES.map(formatPackageLabel)
}
