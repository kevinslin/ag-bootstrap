import { formatGreeting } from "@{{package_scope}}/common"

export function renderHelloWorld(name = "world"): string {
  return formatGreeting(name)
}

if (require.main === module) {
  console.log(renderHelloWorld())
}
