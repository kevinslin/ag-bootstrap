{
  "name": "@{{package_scope}}/integration",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "clean": "rimraf dist coverage tsconfig.tsbuildinfo",
    "dev": "tsc -b -w tsconfig.build.json",
    "format": "prettier --write .",
    "lint": "eslint . --max-warnings=0",
    "test": "jest --config ../../jest.config.cjs --selectProjects integration --runInBand"
  },
  "dependencies": {
    "@{{package_scope}}/client": "workspace:*",
    "@{{package_scope}}/db": "workspace:*",
    "@{{package_scope}}/server": "workspace:*"
  }
}
